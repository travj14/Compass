# macOS
.DS_Store

# Xcode
*.xcuserstate
**/xcuserdata/
build/
DerivedData/

# Compass server runtime (per-machine state — not source)
server/data.json
server/server.log
server/server.pid

# Claude local settings
.claude/settings.local.json
